
query GetRepositoryDetails($owner: String!, $name: String!, $first: Int, $after: String) {

    repository(owner: $owner, name: $name) {
        name
        owner {
            login
        }
        description
        stargazerCount
        forkCount
        createdAt
        updatedAt
        primaryLanguage {
            name
        }
        licenseInfo {
            name
        }
        url

        issues(first: $first, after: $after) {
            pageInfo {
                endCursor
                hasNextPage
            }
            edges {
                node {
                    body
                    title
                    createdAt
                    url
                    title
                    state
                    author{
                        login
                    }
                    labels{
                        edges {
                            node {
                                name
                            }
                        }
                    }

                }
            }
        }

        forks {
            totalCount
        }
    }
}
